# Interactive Feedback MCP - Requirements
# 这个文件从 pyproject.toml 自动生成，用于向后兼容
# This file is auto-generated from pyproject.toml for backward compatibility

# 核心依赖 / Core dependencies
fastmcp>=2.0.0
psutil>=7.0.0
pyside6[multimedia]>=6.8.2.1
pyperclip>=1.8.2
Pillow>=9.0.0
pywin32>=228; sys_platform == "win32"
openai>=1.0.0

# 开发依赖 / Development dependencies (可选 / optional)
# 如果需要开发功能，请使用: uv pip install -r requirements.txt -r requirements-dev.txt
# For development features, use: uv pip install -r requirements.txt -r requirements-dev.txt
